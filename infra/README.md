# infra

Infrastructure-as-code, deployment configuration, and environment setup for
**desk**.

This is where deployment manifests, container definitions, environment
templates (`*.example`), and provisioning scripts live. Real secret/env files
are git-ignored — commit only example templates.

_No infrastructure defined yet._
