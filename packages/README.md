# packages

Shared, reusable libraries used across multiple projects in **desk**.

Anything needed by more than one app or service — data models, API clients,
utilities, design tokens — lives here instead of being copied. Each package is
its own subfolder with its own `README.md`.

_No packages yet._
